# README Quick Start Audit (phase 15, 2026-09-23)

Verdict: already present, line 36-60, skip.

`README.md` line 36 opens with `## Quick start`. It shows the two-line
bootstrap + demo sequence (`npm run bootstrap`, `npm run demo`), names the
Node.js 22.12+ and Python 3.11+ requirements in the preceding
`## Requirements` section (line 25), and follows with the expected pass-path
stdout. Three concrete steps plus expected output; nothing to add.
